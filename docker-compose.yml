services:
  api:
    container_name: rust-api
    build: .
    ports:
      - "8080:8080"
    depends_on:
      - db
  db:
    image: postgres:15.2-alpine
    restart: always
    ports:
      - "5432:5432"
    volumes:
      - db:/var/lib/postgresql/data

volumes:
  db:
    driver: local